# Guide

Results from comparing performance of tuning the hyperparameters using the autotune function from the Fasttext model and using the cross validation function with Optuna are placed in 'autotune_text' and 'cv_text'.

Results from comparing performance of including the company names in addition to their describtions are placed in 'autotune_text' and 'autotune_text_navn' folders. Assuming that the Autotune method from fasttext is better than  the cv method for hyperparameter tuning.